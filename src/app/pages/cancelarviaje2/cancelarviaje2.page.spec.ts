import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Cancelarviaje2Page } from './cancelarviaje2.page';

describe('Cancelarviaje2Page', () => {
  let component: Cancelarviaje2Page;
  let fixture: ComponentFixture<Cancelarviaje2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Cancelarviaje2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
